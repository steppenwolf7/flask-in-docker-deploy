Flask App Deployment (Rocky Linux)

This project uses a simple manual workflow to deploy a Dockerized Flask application to a Rocky Linux server using Git. DevOps trening.


The server is my Intel NUC. It's turned off for now.
